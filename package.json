{
  "name": "poker-post-flop",
  "version": "0.1.0",
  "private": true,
  "description": "Poker post-flop course trainer — 12 lessons, web + Capacitor",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "build:www": "node tools/build-www.js",
    "app:sync": "node tools/build-www.js && cap copy",
    "app:android": "node tools/build-www.js && cap copy android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0"
  }
}
